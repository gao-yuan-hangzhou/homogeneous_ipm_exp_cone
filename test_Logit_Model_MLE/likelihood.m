function likelihood_value = likelihood(alpha_var, beta_var, purchase_record_data)
% Given estimates alpha and beta, compute the likelihood of the testing data
N=size(purchase_record_data,1); Gamma = 0.1*N;
% The likelihood is the optimal objective of a minimization problem with variables z(n,j)

end
